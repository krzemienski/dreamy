---
name: Bug report
about: Report something dreamy is doing wrong
title: "[BUG] "
labels: bug
assignees: ""
---

## Environment

- **dreamy version:** <!-- `dreamy --version` -->
- **Python version:** <!-- `python3 --version` -->
- **OS:** <!-- e.g. macOS 15.x, Ubuntu 24.04, etc. -->

## Command

The exact command you ran (redact any sensitive arguments — file paths
under your home directory are fine, secrets are not):

```
dreamy <subcommand> ...
```

## What happened (real output)

Paste the **actual** stdout/stderr, not a paraphrase. If the output is
long, attach it as a file or a collapsible `<details>` block.

```
paste real output here
```

## What you expected instead

## `dreamy doctor` output

Run `dreamy doctor` and paste its output here. If the bug prevents doctor
from running, say so explicitly instead of leaving this blank.

```
paste dreamy doctor output here
```

## Additional context

Anything else that might help — recent config changes, which connectors
are in play (claude / codex / omp / opencode / pi / nine_router), whether
this is a fresh install or an upgrade, etc.
